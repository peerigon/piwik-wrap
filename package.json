{
  "name": "piwik-client",
  "version": "0.0.1",
  "description": "An easy to use wrapper for the piwik-client.",
  "main": "PiwikClient.js",
  "scripts": {
    "test": "mocha ./test/*.test.js ./test/**/*.test.js --compilers js:babel/register",
    "watch-test": "mocha ./test/*.test.js ./test/**/*.test.js --compilers js:babel/register --watch --growl ",
    "build": "babel ./src/piwik-client.js -o ./piwik-client.js"
  },
  "keywords": [
    "piwik",
    "client",
    "browser",
    "tracking"
  ],
  "author": "Paul Torka",
  "license": "ISC",
  "dependencies": {
    "babel": "^5.8.23"
  },
  "devDependencies": {
    "express": "^4.13.3",
    "jsdom": "^6.5.1",
    "mocha": "^2.3.3",
    "unexpected": "^10.0.1"
  }
}
